% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_local.R
\name{plot_local}
\alias{plot_local}
\title{Plot local correlation and gene expression for significant spots}
\usage{
plot_local(
  res_list,
  name,
  area_name = NULL,
  area_id = NULL,
  plot_type = "all",
  cov = NULL
)
}
\arguments{
\item{res_list}{A list containing results, including \code{test_res}, \code{gene_expr}, and \code{cov_mat}.}

\item{name}{A character string specifying the gene pair's name for which to plot the data.}

\item{area_name}{An optional character string indicating the annotation column name for subsetting (e.g., region name).}

\item{area_id}{An optional vector specifying the annotation values for filtering (\code{area_name} column). Should be categorical.}

\item{plot_type}{A character string specifying the type of plot to create. Options are \code{"rho"} (local correlations), \code{"all"} (both correlation and expression), and \code{"pval"} (local p-values). Default is \code{"all"}.}

\item{cov}{Optional data frame for covariates used in the plot. If not specified, \code{res_list$cov_mat} is used.}
}
\value{
A ggplot object representing the selected type of visualization.
}
\description{
This function generates different types of visualizations for local correlation and gene expression
data, including correlation plots, expression plots, and local p-value significance plots.
}
\examples{
# Assuming `res_list` contains test results and gene expressions:
plot_rho <- plot_local(res_list, name = "geneA_geneB", plot_type = "rho")
plot_all <- plot_local(res_list, name = "geneA_geneB", area_name = "region", area_id = "region1", plot_type = "all")
}
