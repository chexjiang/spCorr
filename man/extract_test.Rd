% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_summary.R
\name{extract_test}
\alias{extract_test}
\title{Extract test results from a list of model test results}
\usage{
extract_test(res, name)
}
\arguments{
\item{res}{A list containing test results for multiple models, where \code{test_res} is a list of individual model test results.}

\item{name}{A character string indicating whether to extract "global" or "local" test results.}
}
\value{
A data frame containing the extracted test results:
\itemize{
\item If \code{name} is "global", returns a data frame with \code{global_fdr}, \code{global_pval}, and \code{edf}.
\item If \code{name} is "local", returns FDR-adjusted p-values (\code{local_fdr}) and z-scores (\code{local_zval}).
}
}
\description{
This function extracts either global or local test results from a list of model test results.
For global results, it extracts global p-values, EDFs, and calculates FDR-adjusted p-values.
For local results, it extracts p-values and z-scores for each gene pair.
}
\examples{
# Assuming `res` is a list containing model test results:
global_results <- extract_test(res, name = "global")
local_results <- extract_test(res, name = "local")
}
